{
  "name": "tiandev.net",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxt/content": "^latest_version",
    "@nuxtjs/pwa": "^latest_version",
    "@nuxtjs/robots": "^latest_version",
    "@nuxtjs/sitemap": "^latest_version",
    "core-js": "^latest_version",
    "nuxt": "^latest_version",
    "vue-disqus": "^latest_version",
    "vue-social-sharing": "^latest_version",
    "vuejs-paginate": "^latest_version",
    "vuex": "^latest_version"
  },
  "devDependencies": {
    "@nuxt/types": "^latest_version",
    "@nuxt/typescript-build": "^latest_version",
    "@nuxtjs/google-analytics": "^latest_version",
    "fibers": "^latest_version",
    "push-dir": "^latest_version",
    "sass": "^latest_version",
    "sass-loader": "^latest_version"
  }
}
